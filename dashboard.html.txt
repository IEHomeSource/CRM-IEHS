<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | CRM</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .section { display: none; }
        .section.active { display: block; }
        .sidebar-item.active { background-color: #1e293b; border-left: 4px solid #3b82f6; color: white; }
    </style>
</head>
<body class="bg-slate-50 text-slate-800">

    <div class="flex h-screen overflow-hidden">
        <!-- SIDEBAR -->
        <aside class="w-64 bg-slate-900 text-slate-300 flex flex-col h-full shadow-xl">
            <div class="p-6 text-xl font-bold border-b border-slate-800 text-white flex items-center gap-2">
                <i class="fa-solid fa-bolt text-blue-400"></i> CRM ALPHA
            </div>
            <nav class="flex-1 mt-4">
                <button onclick="showSection('Leads')" id="navLeads" class="sidebar-item active w-full flex items-center py-3 px-6 hover:bg-slate-800 transition">
                    <i class="fa fa-user-group w-6"></i> Leads
                </button>
                <button onclick="showSection('Tasks')" id="navTasks" class="sidebar-item w-full flex items-center py-3 px-6 hover:bg-slate-800 transition">
                    <i class="fa fa-list-check w-6"></i> Team Tasks
                </button>
            </nav>
            <div class="p-4 border-t border-slate-800">
                <button id="logoutBtn" class="flex items-center gap-2 text-slate-400 hover:text-white transition w-full">
                    <i class="fa fa-right-from-bracket"></i> Logout
                </button>
            </div>
        </aside>

        <!-- MAIN AREA -->
        <main class="flex-1 flex flex-col overflow-y-auto">
            <header class="h-16 bg-white border-b flex items-center justify-between px-8 shadow-sm">
                <h2 id="currentTabTitle" class="text-xl font-bold text-slate-700">Leads Pipeline</h2>
                <div class="flex items-center gap-4">
                    <!-- HIDDEN CSV INPUT -->
                    <input type="file" id="csvFileInput" accept=".csv" class="hidden" />
                    <button onclick="document.getElementById('csvFileInput').click()" class="bg-emerald-50 text-emerald-700 px-4 py-2 rounded-lg text-sm font-bold border border-emerald-200">
                        <i class="fa fa-file-import"></i> Bulk Import
                    </button>
                    <button onclick="openModal('leadModal')" class="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-bold shadow-lg shadow-blue-200">
                        + New Lead
                    </button>
                </div>
            </header>

            <div class="p-8">
                <!-- LEADS SECTION -->
                <section id="sectionLeads" class="section active">
                    <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
                        <table class="w-full text-left border-collapse">
                            <thead class="bg-slate-50 border-b">
                                <tr>
                                    <th class="p-4 text-xs font-bold text-slate-500 uppercase">Name</th>
                                    <th class="p-4 text-xs font-bold text-slate-500 uppercase">Tags</th>
                                    <th class="p-4 text-xs font-bold text-slate-500 uppercase">Status</th>
                                    <th class="p-4 text-xs font-bold text-slate-500 uppercase text-center">Actions</th>
                                </tr>
                            </thead>
                            <tbody id="leadsTableBody"></tbody>
                        </table>
                    </div>
                </section>

                <!-- TASKS SECTION -->
                <section id="sectionTasks" class="section">
                    <div class="flex justify-between items-center mb-6">
                        <h3 class="text-lg font-bold">Team Assignments</h3>
                        <button onclick="openModal('taskModal')" class="bg-indigo-600 text-white px-4 py-2 rounded-lg font-bold">+ New Task</button>
                    </div>
                    <div id="tasksTableContainer" class="bg-white rounded-xl border p-4 shadow-sm">
                        <!-- Tasks list here -->
                        <div id="tasksList" class="space-y-3"></div>
                    </div>
                </section>
            </div>
        </main>
    </div>

    <!-- MODAL: EDIT LEAD (Single View) -->
    <div id="editLeadModal" class="fixed inset-0 bg-slate-900/60 flex items-center justify-center hidden z-50">
        <div class="bg-white p-8 rounded-2xl w-full max-w-lg shadow-2xl">
            <h3 class="text-xl font-bold mb-6">Lead Details & Edit</h3>
            <input type="hidden" id="editLeadId">
            <div class="grid grid-cols-2 gap-4">
                <div class="col-span-2">
                    <label class="text-xs font-bold text-slate-500">FULL NAME</label>
                    <input type="text" id="editLeadName" class="w-full p-3 border rounded-lg">
                </div>
                <div>
                    <label class="text-xs font-bold text-slate-500">TAGS (Comma separated)</label>
                    <input type="text" id="editLeadTags" placeholder="Inverstment, Urgent" class="w-full p-3 border rounded-lg">
                </div>
                <div>
                    <label class="text-xs font-bold text-slate-500">STATUS</label>
                    <select id="editLeadStatus" class="w-full p-3 border rounded-lg">
                        <option value="New">New</option>
                        <option value="Negotiating">Negotiating</option>
                        <option value="Closed">Closed</option>
                    </select>
                </div>
            </div>
            <div class="flex justify-end gap-3 mt-8">
                <button onclick="closeModal('editLeadModal')" class="text-slate-500 font-medium">Cancel</button>
                <button id="updateLeadBtn" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-bold">Update Lead</button>
            </div>
        </div>
    </div>

    <!-- MODAL: NEW LEAD -->
    <div id="leadModal" class="fixed inset-0 bg-slate-900/60 flex items-center justify-center hidden z-50">
        <div class="bg-white p-8 rounded-2xl w-full max-w-md shadow-2xl">
            <h3 class="text-xl font-bold mb-6">Create New Lead</h3>
            <div class="space-y-4">
                <input type="text" id="leadName" placeholder="Name" class="w-full p-3 border rounded-lg">
                <input type="text" id="leadAddress" placeholder="Address" class="w-full p-3 border rounded-lg">
                <select id="leadStatus" class="w-full p-3 border rounded-lg">
                    <option value="New">New</option>
                    <option value="Negotiating">Negotiating</option>
                </select>
            </div>
            <div class="flex justify-end gap-3 mt-8">
                <button onclick="closeModal('leadModal')" class="text-slate-500 font-medium">Cancel</button>
                <button id="saveLeadBtn" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-bold">Save</button>
            </div>
        </div>
    </div>

    <script type="module" src="app.js"></script>
    <script>
        function showSection(name) {
            document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
            document.querySelectorAll('.sidebar-item').forEach(b => b.classList.remove('active'));
            document.getElementById('section' + name).classList.add('active');
            document.getElementById('nav' + name).classList.add('active');
            document.getElementById('currentTabTitle').innerText = name + " Management";
        }
        function openModal(id) { document.getElementById(id).classList.remove('hidden'); }
        function closeModal(id) { document.getElementById(id).classList.add('hidden'); }
    </script>
</body>
</html>